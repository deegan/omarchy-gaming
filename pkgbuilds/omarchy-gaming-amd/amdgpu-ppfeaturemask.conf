# omarchy-gaming: unlock all amdgpu PowerPlay features (overclocking, fan
# control, power limit adjustment). Without this, GPU control tools like
# CoreCtrl/LACT can only read stats, not change anything.
options amdgpu ppfeaturemask=0xffffffff
